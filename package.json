{
  "name": "personality-spark",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd personality-spark-api && npm install",
    "generate-sitemap": "node scripts/generate-sitemap.js"
  },
  "dependencies": {
    "xml2js": "^0.6.2"
  }
}
