{
  "name": "personality-spark",
  "version": "0.1.0",
  "description": "Personality Spark - AI-powered personality discovery platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:frontend": "cd apps/web && npm install && npm run build:cloudflare",
    "build:api": "cd personality-spark-api && npm install && npm run build",
    "deploy:frontend": "cd apps/web && ./deploy-cloudflare.sh",
    "install:all": "npm install && cd apps/web && npm install && cd ../../personality-spark-api && npm install"
  },
  "keywords": [
    "personality",
    "quiz",
    "ai",
    "assessment"
  ],
  "author": "Personality Spark Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}