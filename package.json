{
  "name": "personality-spark",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "postinstall": "cd personality-spark-api && npm install && cd ../apps/web/react-app && npm install",
    "generate-sitemap": "node scripts/generate-sitemap.js"
  },
  "dependencies": {
    "xml2js": "^0.6.2"
  }
}
